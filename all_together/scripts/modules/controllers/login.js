import renderLogin from '../../../../all_together/scripts/modules/views/login.js';
// import { storage, clearStorage } from '../localStorage.js';

export default (context) => {
  renderLogin(context);
};
